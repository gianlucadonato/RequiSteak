<div class="row">
  <div class="small-12 small-centered columns">

		<h1>New Class</h1>

		<%= render 'form' %>

		<%= unless params[:parent_id].nil?
					link_to '<-- Back', unit_path(params[:parent_id]) 
				else
					link_to '<-- Back',  units_path 
				end
		%>

	</div>
</div>
