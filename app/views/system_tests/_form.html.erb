<%= form_for(@system_test) do |f| %>
  <% if @system_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@system_test.errors.count, "error") %> prohibited this system_test from being saved:</h2>

      <ul>
      <% @system_test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <%= hidden_field_tag :r_id, "#{params[:r_id]}" %><br>
  <div class="row">
    <div class="small-12 columns">
      <label><strong>STATUS</strong></label>
      <br>
      <%= radio_button("system_test", "status", "false")%><label class="alert label">Pending</label>
      <%= radio_button("system_test", "status", "true")%><label class="success label">Approved</label>  
    </div>
  </div>
  <br>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit class: "button" %>
  </div>
<% end %>
