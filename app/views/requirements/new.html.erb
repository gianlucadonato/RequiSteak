<div class="row">
  <div class="small-12 small-centered columns">

		<h2>NEW REQUIREMENT</h2>

		<%= render 'form' %>


		<%= unless params[:parent_id].nil?
					link_to '<-- Back', requirement_path(params[:parent_id]) 
				else
					link_to '<-- Back', requirements_path 
				end
		%>
	
	</div>
</div>